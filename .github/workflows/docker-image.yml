name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag aron918/my-portifello-ci-cd-git-workflow:${{ github.sha }}
      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Push Docker image to Docker Hub
        run: docker push aron918/my-portifello-ci-cd-git-workflow:${{ github.sha }}

  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3
  #     - name: Update Kubernetes deployment manifest
  #       run: |
  #         sed -i 's|old-image-name:old-tag|my-image-name:$(date +%s)|' deployment.yaml
  #         git config --global user.name "GitHub Actions"
  #         git config --global user.email "<>"
  #         git commit -am "Update Docker image"
  #         git push origin main
